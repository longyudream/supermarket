## 服务端口
server:
  port: 10000

## 配置数据源
spring:
  datasource:
    username: root
    password: root
    url: jdbc:log4jdbc:mysql://120.79.166.45:3306/supermarket_sms
    ## 可以打印详细sql的驱动
    driver-class-name: net.sf.log4jdbc.DriverSpy
  ##driver-class-name: com.mysql.jdbc.Driver

## 扫描.xml文件
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
